# Normalize line endings to LF across platforms.
* text=auto eol=lf

# Shell hooks must stay LF or the shebang breaks on checkout.
.githooks/* text eol=lf

# Binary assets — never touch.
*.png binary
*.ico binary
*.icns binary
*.woff binary
*.woff2 binary
